# Lead capture endpoint (AWS Lambda Function URL -> SES).
#
# Not a secret: it ships in the client bundle by design. The endpoint is
# CORS-locked to this site's origin and rate limited server-side.
#
# Deployed by infra/deploy-lambda.sh, which prints this value. If the function
# URL is ever deleted and recreated, the hostname changes and this must be
# updated to match.
VITE_LEADS_ENDPOINT=https://2cdeuo4pidhxvwwmpvux5kqmqm0qtghh.lambda-url.us-east-2.on.aws/
